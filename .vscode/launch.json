{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Scanner Main",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/cmd/scanner/main.go"
        }
    ]
}